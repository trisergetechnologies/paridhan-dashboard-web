# Backend API base URL (must include /api/v1 — same as paridhan-next-UI).
# Default: API gateway 4601, or direct backend 4600 (see paridhan-api-gateway / paridhan-backend).
NEXT_PUBLIC_API_URL=http://localhost:4601/api/v1
